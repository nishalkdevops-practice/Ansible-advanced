pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                sh 'mvn --version'
            }
        }
    }
}

        stage('Test') {
            steps {
                echo "Testing happening"
            }
        }

        stage('deploy') {
            steps {
                echo "Deployed successfully"
            }
        }